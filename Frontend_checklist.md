# 🎨 Full-Stack Web UI: HTML5, CSS3 & Streamlit Checklist
# Tracking: [x] = Completed | [ ] = In Progress / Pending

---

## 🏛️ Phase 1: HTML5 Foundations (The Skeleton)
- [x] Milestone 1: Document Architecture (`<!DOCTYPE html>`, `<html>`, `<head>`, `<body>`, `<meta>`)
- [x] Milestone 2: Text Hierarchy & Typography (`<h1>`-`<h6>`, `<p>`, `<span>`, `<strong>`, `<em>` `<hr>`, `<br>`)
- [x] Milestone 3: Content Containers & Lists (`<div>`, `<ul>`, `<ol>`, `<li>`, `<section>`)
- [ ] Milestone 4: Interactive Elements & Media (`<a href="..." target="_blank">`, `<button>`, `<img src="..." alt="...">`)
- [ ] Milestone 5: Native Forms & Inputs (`<form>`, `<input>` types: text/number/date, `<select>`, `<option>`)

---

## 🎨 Phase 2: CSS3 Fundamentals (The Skin & Styling)
- [ ] Milestone 6: CSS Insertion & Selectors (Inline `style=""`, `<style>`, Classes `.name`, IDs `#name`, Tag selectors)
- [ ] Milestone 7: Colors & Typography (HEX/RGBA, `linear-gradient()`, fonts, sizes, weights, alignment)
- [ ] Milestone 8: The CSS Box Model (`content` ➔ `padding` ➔ `border` ➔ `margin`)
- [ ] Milestone 9: Borders, Radii & Shadows (`border-radius`, `box-shadow`, `:hover` effects)
- [ ] Milestone 10: Display Modes & Flexbox Basics (`block`, `inline-block`, `display: flex`, alignment)

---

## ⚡ Phase 3: Streamlit Fundamentals (The Python Engine)
- [ ] Milestone 11: Execution Lifecycle (Understanding top-to-bottom script reruns)
- [ ] Milestone 12: Display Components (`st.title()`, `st.subheader()`, `st.write()`, `st.metric()`)
- [ ] Milestone 13: Input Widgets (`st.text_input()`, `st.number_input()`, `st.selectbox()`, `st.date_input()`, `st.button()`)
- [ ] Milestone 14: Tabular Data Rendering (`st.dataframe()`, `st.table()`, DataFrame integration)
- [ ] Milestone 15: Status & Feedback Alerts (`st.success()`, `st.error()`, `st.warning()`, `st.info()`, `st.balloons()`)

---

## 🧩 Phase 4: Streamlit Layouts & State Control
- [ ] Milestone 16: UI Scaffolding (`st.columns()`, `st.tabs()`, `st.expander()`, `st.sidebar`)
- [ ] Milestone 17: Form Batching (`st.form()`, `st.form_submit_button()`)
- [ ] Milestone 18: Session State Management (`st.session_state` reading, setting, and widget binding)
- [ ] Milestone 19: Navigation & Flow Control (`st.rerun()`, `st.stop()` for access gates)

---

## 🌉 Phase 5: The Hybrid Bridge (Custom HTML/CSS inside Streamlit)
- [ ] Milestone 20: Unsafe Markdown Injection (`st.markdown(..., unsafe_allow_html=True)`)
- [ ] Milestone 21: Streamlit DOM Styling (Targeting metric cards, tabs, and action buttons)
- [ ] Milestone 22: Component Engineering (Building festive cards, badges, and responsive containers)
- [ ] Milestone 23: URL Construction & Encoding (`urllib.parse.quote` for WhatsApp click-to-chat links)